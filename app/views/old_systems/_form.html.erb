<%= form_for(@old_system) do |f| %>
  <% if @old_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@old_system.errors.count, "error") %> prohibited this old_system from being saved:</h2>

      <ul>
      <% @old_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
